# shuffle

Gets an array and return a shuffled version of it

import:
```javascript
import { shuffle } from '@fullstacksjs/toolbox'
```

usage:
```javascript
shuffle(array)
```

example:
```javascript copy
copyArray([]);                // []
copyArray([1]);               // [1]
copyArray([1, 2, 3 ,4 , 5]);  // [2, 1, 3, 4, 5]
```
