# copyArray

Gets an array and return shallow copy version of it.

import:
```typescript copy
import { copyArray } from '@fullstacksjs/toolbox'
```

usage:
```typescript copy
copyArray(array)
```

example:
```typescript copy
copyArray([]);   // []
copyArray([1]);  // []
```
