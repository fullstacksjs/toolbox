# not

returns the "NOT" of its argument.

import:
```typescript copy
import { not } from '@fullstacksjs/toolbox'
```

usage:
```typescript copy
not(): boolean
```

example:
```typescript copy
not(true)  // false
not(false) // true
not(0)     // true
```
