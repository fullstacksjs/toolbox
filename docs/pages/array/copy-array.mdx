# copyArray

Gets an array and return shallow copy version of it

import:
```javascript
import { copyArray } from '@fullstacksjs/toolbox'
```

usage:
```javascript
copyArray(array)
```

example:
```javascript copy
copyArray([]);   // []
copyArray([1]);  // []
```
