# Optional

```ts
type name = Optional<number> // number | undefined
```

A type wrapper for values that may or may not be provided. Use this instead of directly writing `T | undefined` for clarity in API definitions or function parameters.
