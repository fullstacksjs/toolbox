/**
 * Returns the complement of its argument.
 *
 * @export
 * @param {any} b
 * @returns {boolean}
 *
 * @example
 *
 * not(true)  // false
 * not(false) // true
 * not(0)     // true
 * not(1)     // false
 */
export function not(b: any): boolean {
  return !b;
}
