# isString

check given value is string or not.

import:
```typescript copy
import { isString } from '@fullstacksjs/toolbox'
```

usage:
```typescript copy
isString(value): boolean
```

example:
```typescript copy
isString()       // false
isString(1)      // false
isString(['1'])  // false
isString('1')    // true
```
