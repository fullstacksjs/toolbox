# Nullable

```javascript
type name = Nullable<string> // null | string
```

Absolutely. Let's write **clear, professional docs** for your TypeScript utility types in the same style as your example for `Nullable`. We'll cover all the types you've added: `Optional`, `NonEmptyArray`, `Mutable`, `Immutable`, `Result`, `AsyncResult`, `Ok`, `Err`, and `pipe`.