# copyArray

Gets an array and return shallow copy version of it

import:
```javascript copy
import { copyArray } from '@fullstacksjs/toolbox'
```

usage:
```javascript copy
copyArray(array)
```

example:
```javascript copy
copyArray([]);   // []
copyArray([1]);  // []
```
